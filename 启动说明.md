# GNN4ID-FlowAnalyzer 后端服务启动指南

## 一、环境准备

### 1. 安装 Python（推荐 3.8+）

如果没有安装 Python，请从 [Python官网](https://www.python.org/downloads/) 下载安装。

### 2. 安装依赖

打开终端（PowerShell 或 CMD），运行：

```powershell
cd "D:\网络流量分析项目\GNN4ID-IDS"
pip install -r requirements.txt
```

如果安装较慢，可以使用国内镜像：

```powershell
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## 二、启动后端服务

### 方法一：直接启动

```powershell
cd "D:\网络流量分析项目\GNN4ID-IDS"
python api_server.py
```

### 方法二：如果 torch 安装失败，使用轻量版

```powershell
# 先安装轻量依赖
pip install flask flask-cors

# 启动服务（会自动跳过 torch 相关功能）
python api_server.py
```

---

## 三、验证服务是否启动

服务启动后，终端会显示：

```
==================================================
🚀 GNN4ID-FlowAnalyzer API 服务启动中...
📡 服务地址: http://localhost:5000
📋 API文档: http://localhost:5000/
==================================================
```

在浏览器中访问：
- http://localhost:5000/ - 查看服务状态
- http://localhost:5000/api/stats - 查看统计数据

---

## 四、配置小程序

### 1. 打开微信开发者工具

### 2. 修改 API 地址（可选）

如果后端服务不在本机，需要修改：

**文件**: `D:\网络流量分析项目\微信小程序\miniprogram\utils\api.js`

```javascript
// 第3行，修改为你的服务器地址
const API_BASE_URL = 'http://你的服务器IP:5000/api';
```

### 3. 允许不校验域名（开发环境）

在微信开发者工具中：
1. 点击左侧 **"详情"**
2. 勾选 **"不校验合法域名..."**
3. 重新编译项目

---

## 五、API 接口列表

| 接口 | 方法 | 说明 |
|------|------|------|
| `/` | GET | 服务状态 |
| `/api/health` | GET | 健康检查 |
| `/api/stats` | GET | 获取统计数据 |
| `/api/traffic/list` | GET | 获取流量列表 |
| `/api/attack/list` | GET | 获取攻击列表 |
| `/api/attack/detect` | POST | 检测单条流量 |
| `/api/attack/batch-detect` | POST | 批量检测流量 |
| `/api/topology` | GET | 获取网络拓扑 |
| `/api/monitor/metrics` | GET | 获取监控指标 |
| `/api/alerts/recent` | GET | 获取最近告警 |

---

## 六、常见问题

### Q1: 端口被占用

如果 5000 端口被占用，可以修改 `api_server.py` 最后一行：

```python
app.run(host='0.0.0.0', port=8080, debug=True)  # 改为 8080
```

同时修改小程序的 `api.js` 中的 `API_BASE_URL`。

### Q2: 连接失败

1. 检查后端服务是否正在运行
2. 检查防火墙设置
3. 确认小程序勾选了"不校验合法域名"

### Q3: 如何连接真实的 GNN 模型？

将训练好的模型文件放入 `D:\网络流量分析项目\GNN4ID-IDS\model\best_model.pt`

然后修改 `api_server.py` 中的 `load_model()` 函数加载模型。

---

## 七、生产环境部署

### 使用 Docker 部署

创建 `Dockerfile`:

```dockerfile
FROM python:3.9-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
EXPOSE 5000
CMD ["python", "api_server.py"]
```

构建运行：

```bash
docker build -t gnn4id-api .
docker run -p 5000:5000 gnn4id-api
```

---

## 八、技术支持

如有问题，请检查：
1. Python 版本（需要 3.6+）
2. 依赖是否安装成功
3. 端口是否被占用
4. 网络连接是否正常
