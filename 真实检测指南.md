# 🛡️ 真实网络数据检测指南

## 系统架构

```
┌─────────────┐     ┌──────────────┐     ┌─────────────┐
│ 您的电脑     │ ──► │ 流量采集器    │ ──► │ 后端API     │
│ (真实流量)   │     │ traffic_     │     │ (分析)      │
│             │     │ collector.py │     │             │
└─────────────┘     └──────────────┘     └─────────────┘
                                              │
                                              ▼
                                        ┌─────────────┐
                                        │ 微信小程序   │
                                        │ (展示结果)   │
                                        └─────────────┘
```

---

## 快速启动

### 步骤1：启动后端服务

```powershell
cd "D:\网络流量分析项目\GNN4ID-IDS"
python api_server.py
```

### 步骤2：启动流量采集器（新开一个终端）

```powershell
cd "D:\网络流量分析项目\GNN4ID-IDS"
python traffic_collector.py
```

### 步骤3：在微信开发者工具中查看

打开小程序，选择"攻击检测"页面，查看实时检测结果。

---

## 采集器模式

### 模式1：模拟攻击模式（推荐用于演示）

运行后会**自动模拟各种网络攻击**：

| 攻击类型 | 说明 | 检测结果 |
|---------|------|---------|
| 端口扫描 | 大量目的端口 | 侦察攻击 |
| DDoS攻击 | 流量突增 | DDoS攻击 |
| 暴力破解 | SSH/FTP爆破 | 暴力破解 |
| SQL注入 | Web攻击载荷 | Web攻击 |
| 正常流量 | 普通网络活动 | 正常流量 |

**效果预览**：
```
✅ [14:30:05] 发送: DDoS攻击
   预期: DDoS攻击 | 检测: DDoS攻击 | 置信度: 0.88 | 等级: high

✅ [14:30:15] 发送: SQL注入
   预期: Web攻击 | 检测: Web攻击 | 置信度: 0.87 | 等级: high

✓ [14:30:25] 发送: 正常流量
   预期: 正常流量 | 检测: 正常流量 | 置信度: 0.95 | 等级: none
```

### 模式2：实时抓包模式（需要Scapy）

捕获电脑**真实的网络流量**进行分析：

```powershell
# 安装Scapy
pip install scapy

# 运行采集器
python traffic_collector.py
# 选择选项 1
```

---

## 真实网络检测功能

### 当前可检测的攻击

| 攻击ID | 攻击名称 | 检测方法 |
|--------|---------|---------|
| 1 | Web攻击 | 载荷特征分析 |
| 2 | 欺骗攻击 | 连接模式分析 |
| 3 | 侦察攻击 | 端口扫描检测 |
| 4 | Mirai | IoT特征检测 |
| 5 | DoS攻击 | 流量速率分析 |
| 6 | DDoS攻击 | 流量异常检测 |
| 7 | 暴力破解 | 登录失败检测 |

### 检测特征

系统提取以下网络特征进行分析：

```
- 源/目的 IP地址
- 源/目的 端口
- 协议类型 (TCP/UDP/HTTP等)
- 数据包大小和数量
- 流量速率 (bytes/s, packets/s)
- 端口扫描模式
- 可疑载荷特征
- 登录失败次数
```

---

## 配置真实数据源

### 方案1：模拟攻击（当前）

运行 `traffic_collector.py` → 选择模式2 → 自动生成模拟攻击

### 方案2：真实抓包

安装Scapy并捕获真实网络流量：
```powershell
pip install scapy
```

### 方案3：网络设备数据

接入企业网络设备（需要额外配置）：
- 防火墙日志
- IDS/IPS告警
- NetFlow/sFlow数据

---

## 故障排除

### 问题1：无法连接API

```
[ERROR] 无法连接后端API
```

**解决**：
1. 确保 `api_server.py` 已启动
2. 检查API地址是否正确（默认：http://localhost:5000）

### 问题2：Scapy安装失败

```
pip install scapy
```

如果安装失败，使用**模拟攻击模式**代替。

### 问题3：小程序无法获取数据

1. 勾选"不校验合法域名"
2. 重启微信开发者工具
3. 刷新项目

---

## 技术原理

### 检测流程

```
1. 流量采集器捕获网络数据
       ↓
2. 提取流量特征（80+维度）
       ↓
3. 发送到后端API
       ↓
4. GNN模型分析（图神经网络）
       ↓
5. 返回检测结果
       ↓
6. 小程序展示告警
```

### GNN模型原理

```
┌────────────┐     ┌────────────┐     ┌────────────┐
│ 网络流量   │ →   │ 构建流量图  │ →   │ GNN模型    │
│ 原始数据   │     │ 节点=IP    │     │ 图卷积分析  │
└────────────┘     │ 边=连接    │     └────────────┘
                   └────────────┘           ↓
                                         ┌────────────┐
                                         │ 攻击类型    │
                                         │ 置信度评分  │
                                         └────────────┘
```

---

## 下一步

1. **运行模拟攻击** 测试系统
2. **查看小程序** 实时告警
3. **配置真实抓包**（可选）

需要帮助请随时询问！
